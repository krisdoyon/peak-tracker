import styles from "./NewEntry.module.scss";
import { Card, CardHeading } from "components/Card";

export const NewEntry = () => {
  return (
    <Card>
      <CardHeading title={"New Entry"} />
    </Card>
  );
};
